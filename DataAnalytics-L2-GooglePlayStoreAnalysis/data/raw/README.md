# Data Directory

This folder contains raw and processed datasets for the Google Play Store Analysis project.

- `raw/`: Stores the raw Google Play Store Apps CSV (`googleplaystore.csv`) and User Reviews CSV (`googleplaystore_user_reviews.csv`).
- `processed/`: Stores cleaned apps dataset (`cleaned_apps.csv`) and sentiment-analyzed user reviews dataset (`cleaned_reviews.csv`).
