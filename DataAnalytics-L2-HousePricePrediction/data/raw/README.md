# Data Directory

This folder contains raw and processed data for the House Price Prediction project.

- `raw/`: Stores the raw Ames Housing benchmark dataset (`house_prices_raw.csv`).
- `processed/`: Stores the cleaned and preprocessed dataset ready for modeling (`house_prices_cleaned.csv`).
