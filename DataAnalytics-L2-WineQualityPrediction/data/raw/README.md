# Data Directory

This folder contains raw and processed datasets for the Wine Quality Prediction project.

- `raw/`: Stores the raw UCI Wine Quality dataset (`wine_quality_raw.csv`).
- `processed/`: Stores the cleaned dataset with target quality binary encoding (`wine_quality_cleaned.csv`).
